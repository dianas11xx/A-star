#include "Q1_2.h"


vector<int> Q1_2::dfs(vector<vector<char>>& graph){
    int n = graph.size();
    seen = vector(n, false);
    seen[0] = true;
    // map the graph to integers
    for(vector<char> edge: graph){

    }
    dfs_helper(graph, 'a');
    return ans;

}
void Q1_2::dfs_helper(vector<vector<char>>& graph, char node){
    
}

